{
  "name": "uikit-project",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --stream --parallel",
    "dev": "lerna run dev"
  },
  "devDependencies": {
    "lerna": "3.14.1"
  }
}